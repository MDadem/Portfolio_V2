# Portfolio Panel — Environment Variables
# NEVER commit the actual .env file — only this example with placeholders

# MongoDB Atlas Connection (Database: Portfolio)
MONGODB_URI=mongodb+srv://miladiadem58_db_user:<55366059>@algoarenadb.uttfcy0.mongodb.net/Portfolio?appName=AlgoArenaDB

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_jwt_secret_here

# SMTP (Brevo) for password reset emails
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_brevo_smtp_user
SMTP_PASS=your_brevo_smtp_password
SMTP_FROM=miladiadem58@gmail.com

# Frontend URL (for reset email links)
FRONTEND_URL=http://localhost:5173

# Developer IP whitelist (comma-separated, exclude from analytics)
WHITELIST_IPS=

# Tracking kill switch (set to "false" to disable tracking)
TRACKING_ENABLED=true
